/**
 * User-visible browser paths for the RAD feature (Portuguese URLs).
 * Keep in sync with `app.routes.ts` and `authenticated.routes.ts`.
 */
export const RELATORIOS_BASE_PATH = '/relatorios';
export const RELATORIOS_PRINT_PATH = '/relatorios/impressao';
